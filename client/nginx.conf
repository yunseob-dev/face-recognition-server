# HTTP (host port 5173 -> container 80): serve app and proxy API
# HTTPS: nginx-ssl.conf.example 참고
server {
    listen 80;
    server_name localhost;

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    location /api/ {
        proxy_pass http://server:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_cache off;
        proxy_set_header Connection '';
        proxy_http_version 1.1;
        client_max_body_size 500M;
        proxy_read_timeout 600s;
    }

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
}

# HTTPS 사용 시: client/nginx-ssl.conf.example 참고 후 /etc/nginx/conf.d/에 추가하고
# 인증서를 client/ssl/에 두고 -v ./client/ssl:/etc/nginx/ssl 로 마운트 (client/ssl/은 .gitignore됨)
