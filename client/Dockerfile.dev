# Frontend 개발용 Dockerfile
FROM node:20-slim
WORKDIR /app

# 패키지 파일 복사 및 의존성 설치
COPY package.json package-lock.json ./
RUN npm ci

# 소스 코드 복사
COPY . .

# Vite 개발 서버 포트
EXPOSE 5173

# 개발 서버 실행 (--host 옵션으로 외부 접속 허용)
CMD ["npm", "run", "dev", "--", "--host"]

